A subagent that inherits your full conversation context instead of starting
fresh. Start one with `/subtask`.

The inheritance is the only difference. It is still a subagent that returns a
result to the caller, so it sits under the subagent leaf rather than beside it.
With agent view turned off, the command for this is `/fork` instead.
