Claude's notes to itself from previous sessions: build commands it learned,
patterns it noticed, mistakes worth not repeating.

The first 200 lines or 25KB, whichever comes first, load into context. You did
not write this file and you do not curate it, which is what separates it from
`CLAUDE.md`. It is per-repository and it stays on your machine.

680 tokens is small, but it is the one startup line that changes on its own
between sessions.
