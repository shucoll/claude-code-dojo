A bundled skill. `/batch` has Claude split one large change into 5 to 30 subagents, each isolated in its own worktree and each
opening a pull request.

It is a packaged use of two mechanisms you already have, subagents and
worktrees, so the decision it represents was already made when you chose
subagents.
