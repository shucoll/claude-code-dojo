Fires once when the session ends. Its matcher tells you why (`clear`, `logout`,
`exit`, and more).

Use it for teardown: stop a service you started at session start, write a
summary log, or clean up scratch files.
