Fires after a tool call succeeds, using the same tool-name matcher as
PreToolUse.

Because the action already happened, this is the react-to-it event: format a
file the moment it is written, run a linter after every edit, or log what
changed.
