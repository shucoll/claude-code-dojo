One shell command running without blocking the conversation.

No agent is spawned, so nothing here coordinates anything. It appears on this
list because "run it in the background" sounds like a request to dispatch a
session. It only starts a command.
