Your personal overrides for this one project. Gitignored, so nothing here reaches your teammates.

Claude Code adds it to git's ignore list when *it* creates the file. If you create it by hand, add it to `.gitignore` yourself.

It beats the committed `settings.json` and your user-level settings, which makes it the right home for a machine-specific path or a permission only you want.

It lives at the repository root, so one file covers sessions started from any subdirectory or worktree.
